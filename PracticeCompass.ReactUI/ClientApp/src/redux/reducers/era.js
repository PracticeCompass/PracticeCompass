import {
} from "../actionTypes/actionTypes";

const INITIAL_STATE = {
};

export function  eraReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}
