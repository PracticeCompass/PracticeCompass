using System.IO;
using PracticeCompass.Messaging.Parsing;

namespace PracticeCompass.Messaging
{
    class Program
    {
        static void Main(string[] args)
        {
            //var ERAParser = new ERAParser();
            //string[] files =
            //Directory.GetFiles(@"C:\PracticeCompas\Inbound", "*.ERA", SearchOption.AllDirectories);
            //for (var f = 0; f < files.Length; f++)
            //{
            //   var fileText= File.ReadAllText(files[f]);
            //    ERAParser.Process835EraMessage(fileText, "_", "~","^");
            //    File.Move(files[f], Path.Combine(@"C:\PracticeCompas\ParsedERA",Path.GetFileName(files[f])));
            //}
            
            //X12_837Gen.Generate837PMessage();
        }
    }
}
